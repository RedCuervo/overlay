create database diagnostico;
use diagnostico;
create table category (
id_category int primary key,
name varchar(45));
create table product (
id_product int primary key,
model varchar(45),
category_id int,
constraint FK_categoryID foreign key (category_id) references category(id_category));

use diagnostico;
insert into category (id_category, name) values (1, 'Ropa');

drop database diagnostico;